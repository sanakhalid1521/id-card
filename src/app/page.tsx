import IDCardComponent from '../app/componenets/idcard'

export default function DisplayIDCard() { // Renaming the default export function
  return (
    <div>
      <IDCardComponent />
    </div>
  );
}
